// Empty on purpose — this is the foundation issue (#7); no feature tables yet.
// Feature issues extend this file with their own `pgTable` definitions, then
// run `pnpm db:generate` to produce a migration for them.
export {};
